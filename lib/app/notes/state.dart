import 'package:flutter/material.dart';

class NotesState with ChangeNotifier {
  //state

  //firebase firestore (DB)
  //=> Write respone to DB
  //=> Get update data for the DB
  //=>Update my state
}
